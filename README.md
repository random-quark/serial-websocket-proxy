# Serial websocket proxy

* Use node>=6.9.5
* Healthchecks are optional, pass URL as an environment variable `HEALTHCHECK_URL` to activate

```
$ HEALTHCHECK_URL=https://hchk.io/UNIQUE_CODE_URL node index.js
```
